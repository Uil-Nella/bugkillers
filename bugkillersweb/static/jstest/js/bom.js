/**
 * Created by dongbin on 2015/6/16.
 */
/*
 BOM
 1.window
 2.location
 window.location 和 document.location 引用的是同一个对象
 location.host 返回服务器名称和端口号
 location.hostname 不带端口号的服务器名称
 location.href 返回当前加载页面的完整url
 patname 返回url中的目录和（或）文件名
 port 返回端口号 如果url中不包含端口号 返回空字符串
 protocol 返回页面使用的协议
 search 返回url的查询字符串 问号开头
 */

function test() {
    document.querySelector('');
    document.querySelectorAll('');
    alert(document.documentMode);
}